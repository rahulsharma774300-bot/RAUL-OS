name: Build Pink Hero V2 APK

on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - 'pink-hero-v2/**'
      - '.github/workflows/pink-hero-v2.yml'

jobs:
  android:
    runs-on: ubuntu-latest
    container:
      image: barichello/godot-ci:4.5
    env:
      HOME: /root

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Android SDK
        run: |
          set -e
          apt-get update -qq
          apt-get install -y -qq fontconfig
          yes | sdkmanager --licenses >/dev/null 2>&1 || true
          sdkmanager "platform-tools" "build-tools;35.0.0" "platforms;android-35"

      - name: Import project
        run: |
          set -e
          godot --headless --path pink-hero-v2 --editor --quit

      - name: Runtime smoke test
        run: |
          set -e
          cd pink-hero-v2
          set +e
          timeout 10s godot --headless --path . > runtime-smoke.log 2>&1
          CODE=$?
          set -e
          cat runtime-smoke.log
          if [ "$CODE" -ne 0 ] && [ "$CODE" -ne 124 ]; then exit "$CODE"; fi
          if grep -Eqi "SCRIPT ERROR|Parse Error|Invalid (call|access)|Attempt to call|not found in base|Cannot call method" runtime-smoke.log; then
            echo "Runtime smoke test failed"
            exit 1
          fi

      - name: Export APK
        run: |
          set -e
          mkdir -p pink-hero-v2/build
          godot --headless --verbose --path pink-hero-v2 --export-debug "Android Debug" "$(pwd)/pink-hero-v2/build/Pink-Hero-V2.apk"
          test -s pink-hero-v2/build/Pink-Hero-V2.apk

          APKSIGNER="$(find /opt/android-sdk /usr/local/lib/android/sdk /usr/lib/android-sdk -type f -name apksigner 2>/dev/null | sort -V | tail -1)"
          if [ -n "$APKSIGNER" ]; then "$APKSIGNER" verify --verbose pink-hero-v2/build/Pink-Hero-V2.apk; fi

          AAPT="$(find /opt/android-sdk /usr/local/lib/android/sdk /usr/lib/android-sdk -type f -name aapt 2>/dev/null | sort -V | tail -1)"
          if [ -n "$AAPT" ]; then
            "$AAPT" dump badging pink-hero-v2/build/Pink-Hero-V2.apk | tee pink-hero-v2/build/apk-badging.txt
            grep -q "application-label:'Pink Hero V2'" pink-hero-v2/build/apk-badging.txt
            grep -q "launchable-activity" pink-hero-v2/build/apk-badging.txt
          fi

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: pink-hero-v2-apk
          path: pink-hero-v2/build/Pink-Hero-V2.apk
          if-no-files-found: error
